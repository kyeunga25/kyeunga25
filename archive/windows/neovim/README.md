# Legacy Windows Neovim config

This configuration was used on Windows in 2023. It is archived for reference and is not the active macOS configuration.
It includes Windows-specific AppData, clangd.exe, and MinGW paths.

The third-party vim-plug loader is intentionally not vendored. Install a current plugin manager separately before reusing this configuration.
